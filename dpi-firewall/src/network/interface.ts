export class NetworkInterface {
    listen(callback: (packet: any) => void): void {
        // Implementation for starting to capture packets
    }

    send(packet: any): void {
        // Implementation for transmitting packets
    }
}